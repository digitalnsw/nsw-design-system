<div class="nsw-tabs js-tabs{{#if fixed}} js-tabs-fixed{{/if}}">
  <ul class="nsw-tabs__list">
    {{#each tabitems}}
    <li><a href="#{{urlHash}}{{../id-suffix}}">{{title}}</a></li>
    {{/each}}
  </ul>
  {{#each tabitems}}
  <section id="{{urlHash}}{{../id-suffix}}" class="nsw-tabs__content">
    {{{content}}}
  </section>
  {{/each}}
</div>
